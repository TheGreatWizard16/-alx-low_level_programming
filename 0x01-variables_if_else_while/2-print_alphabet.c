#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 * Return: Always 0 (success)
 */
int main(void)
{
	char ch;

	for (c = 'a'; ch <= 'z'; ch++)
	{
		putchar(c);
	}

		putchar('\n');
		return (0);
}
